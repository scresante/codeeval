#!/usr/bin/python
from sys import argv
from math import factorial
try:
    FILE = argv[1]
except NameError:
    FILE = 'tests/64'
DATA = open(FILE, 'r').read().splitlines()


steps = [int(q) for q in data]
#for line in DATA:
#    if not line:
#        continue
#    line = int(line)

steps.sort(reverse=True)




print steps
